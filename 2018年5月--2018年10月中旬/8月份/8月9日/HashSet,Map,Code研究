1.HashCode是个Native方法
	不同JVM有不同的实现，并不一定就是存储地址，绝大多数情况下都不是。

2.从整个数据集的角度来看，不同Object的HashCode是很可能一样的，因为hash本身就是在小数据集中的特征映射，出现冲突是很正常的，因此不能以HashCode是否相等作为判断Object相同的依据

	但是如果两个对象的HashCode不同，那么一定是不同的对象，但是在集合类的使用场景下，追求的“内容”的相同，而不是同一个对象


3.因此，再几何类中，想要做“内容”的contains判断，必须既要重写equals()，又要重写hashcode()，这两个方法的共同作用下，“查重“才能真正OK
