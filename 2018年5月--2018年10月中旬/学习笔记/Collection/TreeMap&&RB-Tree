红黑树的性质
	所有节点，不是红色，就是黑色
	根节点是黑色
	如果一个节点是红色，那么它的子节点是黑色
	任何一个节点，到所有后代叶子节点的简单路径上，包含相同数目的黑色节点


	以上属性保证了，红黑树的任何一个节点到所有叶节点的最长路径不会超过最短路径的两倍

很显然，在search上，RBTree的性能不如AVL tree
	RBTree的优势在于insert和delete时候的更改更少
===========================================================================================================================

Java TreeMap就是基于RBTree来实现的
	也就是说一个节点包含一个KV对，同事还包括Color,left,right,parent
	
TreeMap包含一个root Entry，size(int)包含多少节点
